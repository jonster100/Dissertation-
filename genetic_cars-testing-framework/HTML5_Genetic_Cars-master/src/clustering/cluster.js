
function createDataPointCluster(carDataPointType){
	var cluster = {
		var dataTypeID = carDataPointType;
		var dataArray = new Array();
	}
	return cluster;
}

function createClusterInterface(id){
	var cluster = {
		var clusterID = id;
		var arrayOfClusters = new Array();
	}
	return cluster;
}

function sortCluster(cluster){
	return cluster.sort(function(a, b){return a - b});
}

function findOjectNeighbors(id, cluster, range) {
	var neighbors = new Array();
	return neighbors;
}

function findDataPoint(id, cluster){
	
}

function scoreObject(id, cluster){

}